double invertDouble(double value) {
  // If the value is zero, return zero
  if (value == 0.0) {
    return 0.0;
  } else {
    // Otherwise, return the negative of the value to invert the sign
    return -value;
  }
}
